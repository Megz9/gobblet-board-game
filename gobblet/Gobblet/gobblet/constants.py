from enum import Enum
import pygame

class Color(Enum):
    LIGHT = 0
    DARK = 1

WIDTH, HEIGHT = 800, 500
ROWS, COLS = 4, 4
MARGIN = 0.05 * HEIGHT
BOARD_SIZE = min(WIDTH, HEIGHT) - 2 * MARGIN
SQUARE_SIZE = BOARD_SIZE//COLS
BOARD_START = WIDTH//2 - 2*SQUARE_SIZE
LEFT_PANE_START = BOARD_START - 2 * MARGIN - SQUARE_SIZE
RIGHT_PANE_START = BOARD_START + BOARD_SIZE + 2 * MARGIN
BROWN = (139, 35, 35)
BEIGE = (222, 184, 135)
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
BACKGROUND = (71, 23, 23)
FOREGROUND = (102, 36, 36)
